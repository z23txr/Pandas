# Pandas Series 

Explaining the basics of **Pandas Series in Python**.

## Topics Covered
- Creating Series from **list, dictionary, and CSV files**
- Series **attributes** (size, dtype, index, values)
- **Indexing and slicing**
- **Sorting and filtering**
- **Mathematical operations**
- **Boolean indexing**
- Handling **missing values**
- Important methods like `astype()`, `between()`, `clip()`, `dropna()`, `fillna()`, `apply()`
- **Basic plotting**


# Pandas DataFrame

Explaining the basics of **Pandas DataFrames in Python** for data analysis.

## Topics Covered
- Creating DataFrames using **lists, dictionaries, and CSV files**
- DataFrame **attributes** (`shape`, `columns`, `index`, `dtypes`)
- Data exploration using **head(), tail(), info(), describe()**
- **Selecting rows and columns** using `loc` and `iloc`
- **Filtering data** using conditions
- **Adding and modifying columns**
- Handling **missing values and duplicates**
- Data type conversion using `astype()`

## Libraries Used
- Pandas
- NumPy

## Purpose
This notebook is created for **learning and practicing Pandas**.
